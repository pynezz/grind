// Package client will provide the work-leg client: a Go library so a
// non-browser agent can solve a challenge and attach a credit in about ten
// lines, plus (separately, compiled to WASM) the solver behind the HTML
// interstitial for browsers without a native client. See PLAN.md milestone
// M3. The agent case matters more than the browser case for this project.
//
// Not yet implemented.
package client
